{
  "name": "sentra-backend",
  "version": "1.0.0",
  "description": "SENTRA Backend — Oracle DB + WebSocket real-time (standalone EAP server)",
  "main": "sentra-server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node sentra-server.js",
    "dev": "nodemon sentra-server.js",
    "check": "node --check sentra-server.js && node --check lib/evidence-pack.js && node --check lib/hikvision.js",
    "test": "node tools/smoke-test.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "oracledb": "^6.10.0",
    "pdfkit": "^0.19.1",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
